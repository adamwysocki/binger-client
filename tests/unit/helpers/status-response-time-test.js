import {
  statusResponseTime
} from '../../../helpers/status-response-time';
import { module, test } from 'qunit';

module('StatusResponseTimeHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = statusResponseTime(42);
  assert.ok(result);
});
