import {
  statusButton
} from '../../../helpers/status-button';
import { module, test } from 'qunit';

module('StatusButtonHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = statusButton(42);
  assert.ok(result);
});
