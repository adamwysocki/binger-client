import {
  statusTime
} from '../../../helpers/status-time';
import { module, test } from 'qunit';

module('StatusTimeHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = statusTime(42);
  assert.ok(result);
});
