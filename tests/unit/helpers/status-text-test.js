import {
  statusText
} from '../../../helpers/status-text';
import { module, test } from 'qunit';

module('StatusTextHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = statusText(42);
  assert.ok(result);
});
