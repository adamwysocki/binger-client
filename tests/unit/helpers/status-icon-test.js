import {
  statusIcon
} from '../../../helpers/status-icon';
import { module, test } from 'qunit';

module('StatusIconHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = statusIcon(42);
  assert.ok(result);
});
