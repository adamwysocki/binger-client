import Ember from 'ember';

export default Ember.View.extend({
  didInsertElement : function() {
    /*
    this.$('a.left-off-canvas-toggle').click(function() {
      this.$('a.left-off-canvas-toggle').click();
    });
    */
  }
});
